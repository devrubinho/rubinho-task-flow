{
	"mcpServers": {
		"task-master-ai": {
			"type": "stdio",
			"command": "npx",
			"args": [
				"-y",
				"task-master-ai"
			],
			"env": {
				"ANTHROPIC_API_KEY": "sua-chave-aqui",
				"PERPLEXITY_API_KEY": "sua-chave-aqui"
			}
		}
	}
}

// Configuração padrão para projetos usando:
// - Claude (via Cursor)
// - Task Master AI (via MCP)
// - Cursor Rules (via .cursor/rules/)

